package com.group.eBookManagementSystem;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class EBookManagementApplication {

    // define a main method to get access to the entry point of the application
    public static void main(String[] args) {
        // use the SpringApplication.run() method to start the Spring Boot application
        SpringApplication.run(EBookManagementApplication.class, args);
    }

}
